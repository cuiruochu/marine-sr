# 微调训练配置
epochs: 50
batch_size: 16
lr: 1e-5

optimizer:
  name: adam
  params: {}

scheduler:
  name: cosine
  params:
    T_max: 50
    eta_min: 1e-7

loss:
  name: l1

checkpoint:
  every: 10
  save_best: true
  monitor: val_mae
  mode: min