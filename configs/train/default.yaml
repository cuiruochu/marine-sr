# 默认训练配置
epochs: 200
batch_size: 16
lr: 2e-4

# 优化器配置
optimizer:
  name: adam
  params: {}

# 学习率调度器配置
scheduler:
  name: step
  params:
    step_size: 100
    gamma: 0.1

# 损失函数配置
loss:
  name: l1

# 检查点配置
checkpoint:
  every: 10
  save_best: true
  monitor: val_mae
  mode: min