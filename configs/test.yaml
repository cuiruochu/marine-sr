# 测试配置文件
# 运行前根据需要修改此文件

model:
  name: EDSR

dataset:
  marine_param: wind  # wind / mwd / mwp / swh
  upscale: 2          # 2 / 4

paths:
  test_root: /data/marine/Test/wind
  checkpoint: ./checkpoints/EDSR/wind/x2/best.pth
  eval_mask: ./masks/wave_mask.npy